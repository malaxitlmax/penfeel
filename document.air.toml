root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o bin/document cmd/document/main.go"
bin = "bin/document"
delay = 1000
exclude_dir = ["tmp", "vendor", "client"]
include_ext = ["go"]
exclude_regex = ["_test\\.go"]